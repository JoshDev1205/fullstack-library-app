# Migration `20190825181606-init`

This migration has been generated by Joseph Ospina Tafur at 8/25/2019, 6:16:06 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Author"("id" TEXT NOT NULL  ,"name" TEXT NOT NULL DEFAULT '' ,"country" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"))
;

CREATE TABLE "lift"."Book"("id" TEXT NOT NULL  ,"title" TEXT NOT NULL DEFAULT '' ,"description" TEXT NOT NULL DEFAULT '' ,"quantity" INTEGER   ,"price" INTEGER NOT NULL DEFAULT 0 ,"author" TEXT   REFERENCES "Author"(id) ON DELETE SET NULL,PRIMARY KEY ("id"))
;

CREATE UNIQUE INDEX "lift"."Author.id._UNIQUE" ON "Author"("id")

CREATE UNIQUE INDEX "lift"."Book.id._UNIQUE" ON "Book"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190825181606-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,28 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model Author {
+  id      String @default(cuid()) @id @unique
+  name    String
+  country String
+  books   Book[]
+}
+
+model Book {
+  id          String @default(cuid()) @id @unique
+  title       String
+  description String
+  quantity    Int?   @default(0)
+  author      Author
+  price       Int
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190825181606-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190825181606-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
